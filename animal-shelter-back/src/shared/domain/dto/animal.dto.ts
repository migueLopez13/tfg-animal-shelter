export interface AnimalDTO {
  id: string;
  name: string;
  birthdate: string;
  gender: string;
  species: string;
  breed: string;
  size: string;
  dewormed: boolean;
  arrivalDate: string;
}

export interface VaccineDTO {
  id: string;
  name: string;
}